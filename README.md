# Shell, init files, variables and expansions

This project contains scripts for learning about shell initialization files, variables, and expansions.

## Scripts

- **0-alias**: Creates an alias named `ls` that executes `rm -f *`

